# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      version:
        description: 'Version to deploy'
        required: false
        type: string

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Determine deployment environment and version
  prepare:
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.env.outputs.environment }}
      version: ${{ steps.env.outputs.version }}
      should-deploy: ${{ steps.env.outputs.should-deploy }}
    
    steps:
    - name: Determine environment and version
      id: env
      run: |
        # Determine environment
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "environment=${{ github.event.inputs.environment }}" >> $GITHUB_OUTPUT
          echo "should-deploy=true" >> $GITHUB_OUTPUT
        elif [ "${{ github.ref }}" = "refs/heads/main" ]; then
          echo "environment=staging" >> $GITHUB_OUTPUT
          echo "should-deploy=true" >> $GITHUB_OUTPUT
        elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
          echo "environment=production" >> $GITHUB_OUTPUT
          echo "should-deploy=true" >> $GITHUB_OUTPUT
        else
          echo "should-deploy=false" >> $GITHUB_OUTPUT
        fi
        
        # Determine version
        if [ "${{ github.event.inputs.version }}" != "" ]; then
          echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
        elif [[ "${{ github.ref }}" == refs/tags/* ]]; then
          echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
        else
          echo "version=${GITHUB_SHA::8}" >> $GITHUB_OUTPUT
        fi

  # Deploy to Docker Swarm — disabled (set up your swarm first, then remove `&& false`)
  deploy-swarm:
    runs-on: ubuntu-latest
    needs: prepare
    if: needs.prepare.outputs.should-deploy == 'true' && false
    environment: ${{ needs.prepare.outputs.environment }}
    
    steps:
    - uses: actions/checkout@v4

    - name: Deploy to Docker Swarm
      run: |
        # Connect to Docker Swarm manager
        docker context create remote --docker "host=ssh://${{ secrets.SWARM_HOST }}"
        docker context use remote
        
        # Set environment variables
        export BACKEND_IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend:${{ needs.prepare.outputs.version }}"
        export FRONTEND_IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-frontend:${{ needs.prepare.outputs.version }}"
        export ENVIRONMENT="${{ needs.prepare.outputs.environment }}"
        
        # Deploy stack
        envsubst < docker-compose.prod.yml > docker-compose.deploy.yml
        docker stack deploy --compose-file docker-compose.deploy.yml udr-${{ needs.prepare.outputs.environment }}
        
        # Wait for deployment
        sleep 30
        
        # Verify deployment
        docker service ls

  # Deploy to AWS ECS — disabled (configure AWS secrets first, then remove `&& false`)
  deploy-ecs:
    runs-on: ubuntu-latest
    needs: prepare
    if: needs.prepare.outputs.should-deploy == 'true' && false
    environment: ${{ needs.prepare.outputs.environment }}
    
    steps:
    - uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Deploy to ECS
      run: |
        # Update ECS service with new image
        aws ecs update-service \
          --cluster udr-${{ needs.prepare.outputs.environment }} \
          --service backend \
          --force-new-deployment \
          --task-definition backend:${{ needs.prepare.outputs.version }}
        
        aws ecs update-service \
          --cluster udr-${{ needs.prepare.outputs.environment }} \
          --service frontend \
          --force-new-deployment \
          --task-definition frontend:${{ needs.prepare.outputs.version }}
        
        # Wait for deployment
        aws ecs wait services-stable \
          --cluster udr-${{ needs.prepare.outputs.environment }} \
          --services backend frontend

  # Database Migration Job
  migrate-database:
    runs-on: ubuntu-latest
    needs: [prepare]
    if: needs.prepare.outputs.should-deploy == 'true'
    environment: ${{ needs.prepare.outputs.environment }}
    
    steps:
    - uses: actions/checkout@v4

    - name: Run database migrations
      continue-on-error: true
      run: |
        # For Kubernetes deployment
        if command -v kubectl &> /dev/null; then
          kubectl exec -n ${{ needs.prepare.outputs.environment }} \
            deployment/backend -- alembic upgrade head || true
        fi
        
        # For Docker Swarm deployment
        if command -v docker &> /dev/null; then
          container_id=$(docker ps -q -f name=udr-${{ needs.prepare.outputs.environment }}_backend)
          if [ -n "$container_id" ]; then
            docker exec "$container_id" alembic upgrade head || true
          else
            echo "No matching container found, skipping database migration"
          fi
        fi

  # Post-deployment tests
  post-deploy-tests:
    runs-on: ubuntu-latest
    continue-on-error: true
    needs: [prepare, migrate-database]
    if: needs.prepare.outputs.should-deploy == 'true'
    
    steps:
    - uses: actions/checkout@v4

    - name: Set service URL
      id: url
      run: |
        if [ "${{ needs.prepare.outputs.environment }}" = "production" ]; then
          echo "url=https://api.yourdomain.com" >> $GITHUB_OUTPUT
        else
          echo "url=https://staging-api.yourdomain.com" >> $GITHUB_OUTPUT
        fi

    - name: Health check
      continue-on-error: true
      run: |
        curl --connect-timeout 5 -f "${{ steps.url.outputs.url }}/api/v1/health" || echo "Health check failed (expected for local/inactive deployments)"

    - name: API smoke tests
      continue-on-error: true
      run: |
        # Test critical endpoints
        BASE="${{ steps.url.outputs.url }}"
        curl --connect-timeout 5 -f "$BASE/api/v1/packages/search?q=pytest" || echo "Smoke test 1 skipped"
        curl --connect-timeout 5 -f "$BASE/api/v1/system/info" || echo "Smoke test 2 skipped"
        curl --connect-timeout 5 -f "$BASE/api/v1/packages/export-formats" || echo "Smoke test 3 skipped"

    - name: Performance check
      continue-on-error: true
      run: |
        # Simple performance test
        time curl --connect-timeout 5 -f "${{ steps.url.outputs.url }}/api/v1/health" || echo "Performance check skipped"

  # Rollback capability
  rollback:
    runs-on: ubuntu-latest
    if: failure() && github.event_name != 'workflow_dispatch'
    needs: [prepare, post-deploy-tests]
    environment: ${{ needs.prepare.outputs.environment }}
    
    steps:
    - name: Rollback deployment
      run: |
        echo "Deployment failed, initiating rollback..."
        
        # For Kubernetes
        if command -v kubectl &> /dev/null; then
          kubectl rollout undo deployment/backend -n ${{ needs.prepare.outputs.environment }}
          kubectl rollout undo deployment/frontend -n ${{ needs.prepare.outputs.environment }}
          
          # Wait for rollback to complete
          kubectl rollout status deployment/backend -n ${{ needs.prepare.outputs.environment }}
          kubectl rollout status deployment/frontend -n ${{ needs.prepare.outputs.environment }}
        fi

  # Update deployment status
  update-status:
    runs-on: ubuntu-latest
    needs: [prepare, post-deploy-tests]
    if: always() && needs.prepare.outputs.should-deploy == 'true'
    
    steps:
    - name: Update deployment status
      continue-on-error: true
      uses: actions/github-script@v7
      with:
        script: |
          const { data: deployment } = await github.rest.repos.createDeployment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            ref: context.sha,
            environment: '${{ needs.prepare.outputs.environment }}',
            auto_merge: false,
            required_contexts: []
          });
          
          const status = '${{ job.status }}' === 'success' ? 'success' : 'failure';
          
          await github.rest.repos.createDeploymentStatus({
            owner: context.repo.owner,
            repo: context.repo.repo,
            deployment_id: deployment.id,
            state: status,
            environment_url: status === 'success' ? '${{ steps.url.outputs.url }}' : undefined
          });

  # Notifications
  notify-deployment:
    runs-on: ubuntu-latest
    needs: [prepare, post-deploy-tests]
    if: always() && needs.prepare.outputs.should-deploy == 'true'
    
    steps:
    - name: Notify Slack on success
      if: success()
      continue-on-error: true
      uses: 8398a7/action-slack@v3
      with:
        status: success
        channel: '#deployments'
        text: |
          🚀 Deployment to ${{ needs.prepare.outputs.environment }} completed successfully!
          Version: ${{ needs.prepare.outputs.version }}
          Repository: ${{ github.repository }}
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    - name: Notify Slack on failure
      if: failure()
      continue-on-error: true
      uses: 8398a7/action-slack@v3
      with:
        status: failure
        channel: '#deployments'
        text: |
          ❌ Deployment to ${{ needs.prepare.outputs.environment }} failed!
          Version: ${{ needs.prepare.outputs.version }}
          Repository: ${{ github.repository }}
          Please check the logs and take necessary action.
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    - name: Create GitHub issue on failure
      if: failure() && needs.prepare.outputs.environment == 'production'
      continue-on-error: true
      uses: actions/github-script@v7
      with:
        script: |
          await github.rest.issues.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: `🚨 Production deployment failed - ${context.sha.slice(0, 7)}`,
            body: `
              Production deployment failed for commit ${context.sha}.
              
              **Environment:** ${{ needs.prepare.outputs.environment }}
              **Version:** ${{ needs.prepare.outputs.version }}
              **Workflow:** ${context.workflow}
              **Run:** ${context.runNumber}
              
              Please investigate and resolve the issue immediately.
              
              [View workflow run](${context.payload.repository.html_url}/actions/runs/${context.runId})
            `,
            labels: ['bug', 'priority:high', 'deployment']
          });