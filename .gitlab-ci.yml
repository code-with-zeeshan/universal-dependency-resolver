stages:
  - udr-lock

udr-lock-check:
  stage: udr-lock
  image: python:3.12-slim
  timeout: 5 minutes
  before_script:
    - pip install ud-resolver
  script:
    - udr verify
  only:
    - merge_requests
    - main
